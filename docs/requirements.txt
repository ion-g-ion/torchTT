# Tested with Python 3.12. Install CPU-only PyTorch first:
# python -m pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu
# torchTT itself is imported from the selected source checkout, never installed.
Sphinx==8.1.3
sphinx-rtd-theme==3.0.2
nbsphinx==0.9.7
ipython==8.37.0
numpy==2.2.6
scipy==1.15.3
opt-einsum==3.4.0
